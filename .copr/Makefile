# COPR's make_srpm SCM method invokes this file directly from the repository
# root. Keep the packaging implementation in the root Makefile so local and
# COPR SRPM generation use exactly the same rules.
include Makefile
